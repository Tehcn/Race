#ifndef KEYWORDS_H
#define KEYWORDS_H

#include "general.h"
#include <vector>

// im fancy, i know
std::vector<std::string>> get_keywords(void) {
	static const std::vector<std::string>> keywords({
		"int",
		"string",
		"bool",
		"if",
		"for",
		"function",
		"public",
		"private",
		"static",
		"void",
		"const"
	});

	return keywords;
}

class keywords {
public:
	std::vector<std::strig>> static get(void) {
		
	}
}

#endif